# setup.py

from setuptools import setup, find_packages

setup(
    name="profiler_module",
    version="1.0",
    packages=find_packages(),
    install_requires=[],
    tests_require=["unittest"],
)
